# 第二次作业

---

## 代码简介：
* 代码一共包含两个类：
```java
enum CalabashBrother{
    ...
}
public class Sort{
    ...
}
```
### CalabashBrother类
* 改进后用枚举类型描述葫芦娃，并固定葫芦娃为7个对象，比起直接用类来定义，显得更符合常理。并定义了相关方法，以便内部数据的访问。
### Sort类
* Sort类中包含打乱顺序、冒泡排序、二分插入排序、按名字报数、按颜色报数等方法

## main函数
* main函数中，首先创建一个葫芦娃数组，将7个枚举类型的对象添加到数组中。对于排序方法的访问，不再采用静态方法，而是再创建sorter，由对象排序者调用相关方法，先打乱顺序、报数，再分别按两种方法排序、报数。