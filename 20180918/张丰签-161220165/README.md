1.共有3个类，分别名为BrotherAct，即对葫芦娃兄弟的创建、各类操作（初始化位置、移动位置、报告名字和颜色）所包含的类；
Brother，即存储单个葫芦娃信息以及单个葫芦娃的操作的类（报告自身名字、颜色、报告自己位置改变的信息）；
Test，即两个排序函数。
2.从Test中的Main开始执行，创建BrotherAct的对象act，对其初始化、赋予其初始化位置（参数为Int类型的数组），将act作为参数
传给两个排序函数进行操作；排序函数根据自己排序的特点，分别利用act的两个函数接口进行操作。